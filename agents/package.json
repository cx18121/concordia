{
  "name": "agents",
  "private": true,
  "type": "module",
  "description": "6 demo agents: deterministic strategies + LLM thesis layer, voting via Dynamic server wallets.",
  "scripts": {
    "seed": "tsx src/seed.ts",
    "run": "tsx src/run.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "@types/node": "^20.17.6"
  }
}
