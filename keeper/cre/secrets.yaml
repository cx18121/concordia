# Workflow secrets (none needed for replay-mode simulation). For a live deploy, push secrets to
# the Vault DON with `cre secrets create` and reference them here. See CONTRACTS.md §6.
secretsNames: {}
